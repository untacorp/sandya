# Database Design (`docs/design/database`)

Folder ini berisi dokumen spesifikasi skema data, diagram relasi entitas (ERD), skema Drizzle ORM, strategi indeks, dan arsitektur event store.

---

##  Dokumen dalam Folder Ini
*(Dokumen rancangan database akan ditambahkan di sini)*

- Template / Standar:
  - Mermaid Entity-Relationship Diagram (ERD).
  - Skema DDL / Drizzle ORM TypeScript definitions.
  - Skema Payload Event Sourcing (event types, cryptographic signature, hash chaining).
